import Link from 'next/link'
import React from 'react'

const TaxReturnCard = ({ taxType, userName, showBalance }: CreditCardProps) => {
  return (
    <div className='flex flex-col'>
        <Link href='/' className='bank-card'>
            
        </Link>
    </div>
  )
}

export default TaxReturnCard